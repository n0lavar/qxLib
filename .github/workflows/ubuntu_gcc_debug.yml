name: ubuntu_gcc_debug

on: push

jobs:
  ubuntu_gcc_debug:
    runs-on: ubuntu-latest
    env:
      CXX: g++-10

    steps:
      - uses: actions/checkout@v2
      - uses: ./.github/actions/configure_gcc
      - uses: ./.github/actions/install_cmake
      - uses: ./.github/actions/install_dependencies
        with:
          build_type: Debug
      - uses: ./.github/actions/build
        with:
          build_type: Debug
      - uses: ./.github/actions/run_tests
      - uses: ./.github/actions/calc_code_coverage
      